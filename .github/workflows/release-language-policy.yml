name: Release language policy

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  policy:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - name: Test repository policy probes
        run: node --test scripts/release-language-policy.test.mjs
      - name: Scan every tracked text file
        run: node scripts/release-language-policy.mjs
